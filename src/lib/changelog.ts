export type ChangelogEntry = {
  version: string;
  /** ISO 8601 (YYYY-MM-DD) */
  date: string;
  /** 何が変わったか。1項目1行 */
  changes: string[];
  /**
   * どう使うか（どこを開く / 何を押す / どうなれば成功か）。
   * 画面で使える変化が無いリリースでは共有ワークフローが生成しないため、その場合は持たせない
   * （空配列ではなく未定義にする。画面は `usage` が無いときに枠ごと出さない）。
   */
  usage?: string[];
};

/**
 * 設定 →「更新履歴」に表示するデータ。
 *
 * ## 追記の仕方
 *
 * **手で書き足す必要は無い。** develop→mainのリリースフロー
 * （`.github/workflows/release-develop-to-main.yml`）が差分から利用者向けの文面を生成し、
 * バージョンbump時の `version` lifecycleスクリプト（`scripts/version-changelog.mjs`）が
 * この配列の先頭へ新しいエントリを挿入する。生成された文面はバンプPRの本文にも載るため、
 * 内容の確認はそこで行う。
 *
 * ## 記載ルール（手で直すときに守ること）
 *
 * - 利用者が画面を見て体感できる変更だけを書く
 * - 内部実装・リファクタリング・CI/CD・依存関係の更新は書かない
 * - 開発者向けの用語は利用者向けの言い方に言い換える
 * - 過去バージョンのエントリは変更しない
 *
 * 新しい順に並べる。先頭は常に `package.json` の `version` と一致する
 * （`tests/changelog.test.ts` で検証している）。
 */
export const APP_CHANGELOG: ChangelogEntry[] = [
  {
    version: "0.4.0",
    date: "2026-08-28",
    changes: [
      "データベースの一覧・テーブル画面でビュー（VIEW）も表示され、内容を閲覧できるようになりました。また、設定画面から新しいデータベースとデータベース用ユーザーを作成し、権限の変更やパスワードの再発行ができるようになりました。",
    ],
    usage: [
      "1. データベース一覧またはテーブル一覧を開くと、通常のテーブルに加えてビュー（VIEW）も表示され、タップして内容を閲覧できます（編集・削除ボタンは表示されません）。",
      "2. 設定画面を開き、「データベース作成」から `app_` で始まる名前を入力して新しいデータベースを作成します。作成後、一覧に追加されれば成功です。",
      "3. 設定画面の「ユーザー管理」から `app_` で始まる名前でデータベース用ユーザーを作成すると、その場でパスワードが1回だけ表示されます。表示された時点で作成成功です。",
      "4. 同じ画面からユーザーの権限変更やパスワードの再発行、削除も行えます。操作後に一覧の表示が更新されれば反映は完了です。",
    ],
  },
  {
    version: "0.3.0",
    date: "2026-08-27",
    changes: [
      "アプリの見た目を一新しました。表示名を「DB Console」にあらため、ホーム画面やタブに表示されるアイコンを新しいデザインに変更しました。あわせて設定画面から、今使っているアプリのバージョンと、これまでの更新内容をいつでも確認できるようになりました。",
    ],
    usage: [
      "1. スマートフォンのホーム画面にアプリを追加すると、新しいアイコンとアプリ名「DB Console」で表示される",
      "2. アプリの設定画面を開く",
      "3. 画面下部の「アプリ情報」でバージョン番号を確認する",
      "4. 「更新履歴」ボタンを押す",
      "5. これまでの変更内容が新しい順に一覧表示されれば成功",
    ],
  },
  {
    version: "0.2.6",
    date: "2026-08-25",
    changes: [
      "これより前の更新内容は記録していません。次のリリースから、変更点をここに表示します。",
    ],
  },
];
